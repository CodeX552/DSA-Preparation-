# Recursion Basics
## Base case + Recursive case
## Recursion tree imagine karo
## Backtracking = DFS + undo

# Backtracking Patterns
## Choose → Explore → Unchoose
## Subsets: O(2^n), Permutations: O(n!), Combinations: O(C(n,k))
